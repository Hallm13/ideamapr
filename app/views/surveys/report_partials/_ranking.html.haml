%tr{style: tr100 + reportpara + h2font + boldfont}
  %td{style: width12(2) + boldfont}
    Ranking Averages
  %td{style: width12(10) + boldfont}
    Ideas

- qn[:ranking_averages].each do |avg_data|
  %tr
    %td{style: width12(2)}
      = sprintf("%0.2f", (avg_data[:avg] || 0))
    %td{style: width12(10)}
      = avg_data[:title]
