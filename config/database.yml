# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
# Copy this file to database.yml - this is moved out to a sample to prevent accidental commits of your DB creds

development:
  adapter: postgresql
  username: 
  database: ideamapr_dev
  pool: 5
  encoding: unicode
  username: pg_dev
  password: pg_dev_password

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  adapter: postgresql
  database: ideamapr_test
  pool: 5
  encoding: unicode
  username: pg_dev
  password: pg_dev_password
